document.addEventListener('keydown', function (event) {
    // console.log(event.key);
    if (event.key === 'Q' || event.key === 'q') {
        // console.log("ok");   
        alert("警告！");
    }
});